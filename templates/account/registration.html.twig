{% extends "base.html.twig" %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="container full">
        <h1>Register on Drive2Night</h1>
        {{form_start(myForm)}}

            {{form_widget(myForm)}}
            <button type="submit" class='btn btn-success'>Confirm registration</button>
        {{form_end(myForm)}}
    </div>
{% endblock %}