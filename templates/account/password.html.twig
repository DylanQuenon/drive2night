{% extends "base.html.twig" %}

{% block title %}Change user password{% endblock %}

{% block body %}
    <div class="container full">
        <h1>Change password</h1>
        {{form_start(myForm)}}
            {{form_widget(myForm)}}
            <button type="submit" class="btn btn-warning">Confirm password change</button>
        {{form_end(myForm)}}
    </div>
{% endblock %}