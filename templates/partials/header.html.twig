<nav class="navbar navbar-expand-lg" style="background-color: #f50438">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="#">Drive2Night</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('homepage') }}">Home <i class="fa-solid fa-warehouse" style="color: #ffffff;"></i></a>
                </li>

                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('cars_index') }}">Cars <i class="fa-solid fa-car" style="color: #ffffff;"></i></a>
                </li>

                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('cars_brands_list') }}">Brands <i class="fa-solid fa-list" style="color: #ffffff;"></i></a>
                </li>

                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('cars_search') }}">Search <i class="fa-solid fa-magnifying-glass" style="color: #ffffff;"></i></a>
                </li>
            </ul>

            <ul class="navbar-nav ms-auto">
            
                {% if app.user %}
                  <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" id="accountDropdownLink">{{app.user.fullName}} 
                  {% if app.user.picture %}
    <img src="/uploads/{{app.user.picture }}" alt="Photo de profil" class="rounded-circle mx-" style="width: 40px; height: 40px; object-fit: cover;">
{% else %}
    <img src="{{ asset('uploads/user.webp') }}" alt="Photo de profil par défaut" class="rounded-circle mx-" style="width: 40px; height: 40px; object-fit: cover;">
{% endif %}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a href="{{path('cars_create')}}" class="dropdown-item">Post a Car</a>

                        <div class="dropdown-divider"></div>
                        <a href="{{path('account_index')}}" class="dropdown-item">My Account</a>
                        <a href="{{path('account_profile')}}" class="dropdown-item">Edit my Profile</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{path('account_logout')}}" class="dropdown-item text-danger">Log Out</a>
                    </div>
                </li>

                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('account_register') }}" class="nav-link">Registration</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('account_login') }}" class="nav-link">Login</a>
                    </li>
                {% endif %}
             
              
            </ul>
        </div>
    </div>
</nav>
